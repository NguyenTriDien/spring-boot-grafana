server {
    listen 80;
    server_name redis.anhtester.io.vn;

    location / {
        proxy_pass http://redisinsight;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}